<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="35 seconds">

    <!-- To enable JMX Management -->
    <jmxConfigurator/>

    <!-- Off in that app variant (Enable manually) -->
    <appender name="FS" class="ch.qos.logback.core.FileAppender">
        <!--<file>C:/log/applog.log</file>-->
        <file><!-- Your location --></file>

        <encoder>
            <charset>UTF-8</charset>
            <pattern>%date %-5level %logger{50}.%M:%L - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) %cyan(%class{50}.%M:%L) - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.votingsystem" level="debug"/>
    <!--<logger name="org.springframework.web.servlet" level="debug"/>-->
    <logger name="org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver" level="debug"/>
    <!--<logger name="org.springframework.security" level="debug"/>-->
    <!--<logger name="org.springframework.security.web.csrf.CsrfFilter" level="debug"/>-->

    <root level="info">
        <!--<appender-ref ref="FS"/>-->
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>